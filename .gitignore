# ─── .NET / C# ───
bin/
obj/
*.user
*.suo
*.userosscache
*.sln.docstates
.vs/
*.DotSettings.user
publish/
out/

# ─── NuGet ───
*.nupkg
**/[Pp]ackages/*
!**/[Pp]ackages/build/

# ─── IDE ───
.idea/
*.swp
*.swo
*~

# ─── OS ───
.DS_Store
Thumbs.db

# ─── Environment ───
.env
.env.local
.env.*.local
appsettings.Development.json

# ─── Docker ───
docker-compose.override.yml

# ─── Document Storage ───
# Anchor to repo root (leading /) so it matches only the top-level data/ directory used by
# DOCUMENT_STORAGE_PATH — not accidentally the Data/ folders inside src/ or tests/ which hold
# EF Core DbContext code and fixtures. (Git on Windows/macOS is case-insensitive by default.)
/data/

# ─── Test Results ───
TestResults/
coverage/

# AI Workflow System (proprietary — do not publish)
# ⚠️ TRACKED DURING DEV — run /prepare-public before making repo public
# .agent/workflows/
# .agent/guides/
# .agent/agents/
.agent/.last-sync

# Internal project planning
# ⚠️ TRACKED DURING DEV — run /prepare-public before making repo public
# docs/progress.md
docs/build-journal.md
docs/architect_journal.md
# docs/*PRD*.md
# docs/*prd*.md

# YOLO Mode workspace (runtime state, not committed)
.yolo/

# Multi-agent platform context (auto-generated, not committed)
CLAUDE.md
.claude/
AGENTS.md
.cursorrules
